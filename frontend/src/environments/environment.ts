export const environment = {
    production: true,
    // Fallback to localhost during build if no environment variable is provided, 
    // but ideally this should be your deployed backend URL.
    apiUrl: 'https://your-production-backend-url.com/api'
};
